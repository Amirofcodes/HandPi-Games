# Project
PROJECT_NAME=handpi

# Router (Nginx)
ROUTER_PORT=80

# Backend (Flask)
BACKEND_PORT=5001
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change-me-in-production
JWT_SECRET=change-me-in-production
JWT_EXPIRATION=3600  # 1 hour

# Frontend (Vite)
FRONTEND_PORT=3000
NODE_ENV=development
VITE_API_URL=http://localhost/api
VITE_HOST=0.0.0.0

# Database Configuration (Optional, if using a DB)
DB_USER=handpi_user
DB_PASSWORD=change-me-in-production
DB_NAME=handpi_db
DB_HOST=database
DB_PORT=5432
DATABASE_URL=postgresql://handpi_user:change-me-in-production@database:5432/handpi_db

# Logging and Debugging
LOG_LEVEL=DEBUG

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost 