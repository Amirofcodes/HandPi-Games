apiVersion: v1
kind: ConfigMap
metadata:
  name: handpi-config
  namespace: handpi-games
data:
  FLASK_ENV: "production"
  NODE_ENV: "production"
  LOG_LEVEL: "INFO"
  CORS_ALLOWED_ORIGINS: "https://handpi.yourdomain.com"
  VITE_API_URL: "https://handpi.yourdomain.com/api"
