import React from 'react';

const LocalSetup = () => (
  <section className="local-setup">
    <h2>Local Setup</h2>
    <p>Clone the repo and run <code>npm install</code> then <code>npm run dev</code> to start the SPA locally.</p>
  </section>
);

export default LocalSetup; 